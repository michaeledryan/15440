package messages;

/**
 * Enum for the type of request to the registry.
 *
 * @author Michael Ryan and Alex Cappiello
 */
public enum RegistryMessageType {
    BIND, REBIND, UNBIND, LOOKUP, LIST, EXN
}
